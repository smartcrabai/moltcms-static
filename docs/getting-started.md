# Getting started

Install with Bun on Node 22+ and create `moltcms.config.ts`. Run `sync`, then `codegen`, then `build`. A build requires `state.json.status === "complete"` and an exact match between projection and generated schema fingerprints. Use `examples/blog` for an offline fixture-only run.
