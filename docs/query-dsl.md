# Query DSL

Queries are canonical descriptors with `eq`, `ne`, range, `in`, containment, existence, boolean composition, multi-field sort, select, locale, offset, limit, and pagination. Their SHA-256 key and fingerprint include ordered IDs, selected values, boundaries, count, sort, and descriptor version. Opaque queries deliberately depend on the whole content type.
